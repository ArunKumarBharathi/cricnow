import { ErrorModalDirective } from './error-modal.directive';

describe('ErrorModalDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorModalDirective();
    expect(directive).toBeTruthy();
  });
});
